import {Text, View} from "react-native";
import React from "react";

export function LikesScreen(): React.JSX.Element {
    return (
        <View style={{flex: 1, justifyContent: "center", alignItems: "center"}}>
            <Text>Hello from new Likes Screen</Text>
        </View>
    );
}